const User = require('./User');
const TLog = require('./TLog');
const Type = require('./Type');
// Define associations here
// Example:
// User.hasMany(Post);
// Post.belongsTo(User);

module.exports = {
    User,
    TLog,
    Type,
};
